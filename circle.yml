machine:
  services:
    - docker
test:
  pre:
    - chmod +x -r conf/
    - chmod +x -r src/
    - docker-compose build
    - docker-compose up -d
  override:
    - curl --retry 2 --retry-delay 5 -v http://localhost:8085